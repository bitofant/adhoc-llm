# Selected model. Must match a file in config/models/<MODEL>.env
# Override per invocation: MODEL=other ./up.sh
MODEL=deepseek-v41-flash

# Local proxy: where clients point, and the fallback when no remote runtime is up.
PROXY_PORT=8080
LOCAL_FALLBACK_URL=http://127.0.0.1:11434/v1
